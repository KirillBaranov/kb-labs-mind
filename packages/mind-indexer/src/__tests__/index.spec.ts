/**
 * Basic tests for mind-indexer
 */

import { describe, it, expect } from 'vitest';

describe('Mind Indexer', () => {
  it('should export main functions', () => {
    // This is a basic smoke test to ensure the package builds correctly
    expect(true).toBe(true);
  });
});
