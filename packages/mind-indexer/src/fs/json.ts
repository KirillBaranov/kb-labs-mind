/**
 * JSON file operations for KB Labs Mind Indexer
 * Re-exports from @kb-labs/mind-core to avoid circular dependencies
 */

export { readJson, writeJson, computeJsonHash } from "@kb-labs/mind-core";
